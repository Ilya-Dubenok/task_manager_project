spring:
  jackson:
    property-naming-strategy: SNAKE_CASE
  config:
    import: confidential.yml

server:
  port: 82
  servlet:
    context-path: /notification_service
    encoding:
      charset: UTF-8
      force: true
app:
  network:
    user_service:
      address: localhost
      host: localhost
      appendix: /user_service/api/v1/users
    audit_service:
      address: localhost:81
      host: localhost:81
      appendix: /audit_service/api/v1/audit


